import React from 'react'
import Layout from '../components/layout';

const ProjectPage = () => {
    return (
        <Layout>
            <div id="projects">
                <h1>Projects</h1>
            </div>
        </Layout>
    )
}

export default ProjectPage;